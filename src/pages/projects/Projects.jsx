import './Projects.scss';

function Projects() {
    return (
        <div>
            <h1>Projects</h1>
            <p>
                HTML / CSS / Javascript / React / Node / Express / MongoDB /
                Sass / SCSS / Scrum / Agile /{' '}
            </p>
            {console.log('Projects')}
        </div>
    );
}

export default Projects;
